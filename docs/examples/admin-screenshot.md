# Placeholder

Replace this file with a real screenshot of the CMS "Search Data" admin
(`/admin/search-data`): view, filter, delete, and reindex OpenSearch documents.

How to capture (local DDEV):

1. Log into the CMS at `https://<project>.ddev.site/admin`
2. Open `https://<project>.ddev.site/admin/search-data`
3. Screenshot the GridField list (include the toolbar buttons and a filtered
   view for best effect)
4. Save as `admin-screenshot.png` (overwrite this file), keeping the size
   under ~200KB
